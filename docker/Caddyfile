{
    # Email used for Let's Encrypt / ZeroSSL certificate registration
    email bartosz@idct.tech
}

idct.tech, idct.pl {
    root * /app/build
    file_server

    # Hide dotfiles (e.g. .git if it ever ends up here) from public access
    @dotfiles path */.* */.*/
    respond @dotfiles 404

    log {
        output stdout
        format console
    }
}
